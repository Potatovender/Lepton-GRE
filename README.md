# Lepton-GRE


A 2D math equation graph rendering engine! 

Hoping to make available in browser mode in the future

As of now, the graph performs the following process:

(f(x,y)->z->(r,g,b))

In the future I may allow the return of tuple values instead (f(x,y)->(z1,z2)->(r,g,b))
